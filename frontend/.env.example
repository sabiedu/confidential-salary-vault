# Confidential Salary Vault — Frontend environment
# Copy to .env.local and fill VITE_VAULT_ADDRESS after deploying the contract.

# Address of the deployed ConfidentialSalaryVault contract on Sepolia.
# (Deploy with: npx hardhat run scripts/deploy.ts --network sepolia)
VITE_VAULT_ADDRESS=
