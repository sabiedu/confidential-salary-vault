# Confidential Salary Vault — Environment
# Copy to .env and fill in. NEVER commit .env.

# Sepolia RPC (public node works, or use Alchemy/Infura)
SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com

# Deployer private key (generate fresh per entry — our rule)
# Format: 0x... (64 hex chars)
PRIVATE_KEY=
